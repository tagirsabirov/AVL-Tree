# binarytree module used
# binarytree only prints binary search trees
# binarytree does not insert or sort binary trees
from binarytree import Node

#replace with: node above x instead of root
def insert(rootx, x):
    if x == root:
        return rootx
    elif rootx.val > x
        #insert node to the right
        root.right = Node(x)
    if x < root:
        #insert node to the left
        root.left = Node(x)

root x

#BST list
nodes = [1, 2, 3, 4, 5]
